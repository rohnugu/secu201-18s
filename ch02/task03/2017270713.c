#include<stdio.h>

typedef struct matrix{
        int row, col;
} matrix;

typedef struct _elements {
        int row;
        int col;
        int value;
} element;

typedef element* smatrix;


void smadd(smatix a,smatix b, smatrix d){
		// hjroh
		// Col이라는 변수를 선언한 이유가 불분명하네요.
		// 변수명은 대문자로 시작하지 않는 것이 일반적인 관례입니다.	
        int maxCols;
        int Col[maoxCols]= {0};
        for (int i=0 ; i < maxRow; i++)
                for<F5>hn
}