void smadd(matrix a, matrix b, matrix d) {
	// hjroh
	// 기본적인 모양은 틀리지 않았는데 시간이 부족했는지 상세가 없네요.
	int idx = 0;
	for (int i = 0; ; i++) {
		for (int j = 0; ; j++) {
			d[idx].value = a[i].value + b[j].value;
			d[idx].row = 
		}
	}


}
