#include<stdio.h>

typedef struct matrix{
        int row, col;
} matrix;

typedef struct _elements {
        int row;
        int col;
        int value;
} element;

typedef element* smatrix;


void smadd(smatix a,smatix b, smatrix d){
        int maxCols;
        int Col[maoxCols]= {0};
        for (int i=0 ; i < maxRow; i++)
                for<F5>hn
}